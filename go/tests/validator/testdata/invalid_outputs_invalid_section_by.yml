metadata:
  profile: "test-invalid-section-by"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Production/Database"
      key: "Password"

outputs:
  - file: "config.toml"
    environment: "production"
    format: "toml"
    section_by: "invalid_option"
