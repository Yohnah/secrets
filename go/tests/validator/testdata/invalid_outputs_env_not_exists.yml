metadata:
  profile: "test-env-not-exists"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Production/Database"
      key: "Password"

outputs:
  - file: ".env.staging"
    environment: "staging"
    format: "dotenv"
