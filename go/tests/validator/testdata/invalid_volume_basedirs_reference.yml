metadata:
  profile: "test-volumes"
  basedirs:
    production: "non-existent-volume"
environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Database"
      key: "Password"
volumes:
  - name: "existing-volume"
    mount_path: "/var/lib/data"
    type: "dir"