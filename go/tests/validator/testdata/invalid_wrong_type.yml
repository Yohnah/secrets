metadata:
  profile: "myapp"
  default_environment: "production"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "invalid_type"
      entry: "/Production/DB"
      key: "Password"

outputs: []
