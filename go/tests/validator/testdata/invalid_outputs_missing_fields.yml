metadata:
  profile: "test-missing-fields"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Production/Database"
      key: "Password"

outputs:
  - file: ".env"
    environment: "production"
  - file: "set-env.bash"
    environment: "production"
    format: "shell"
