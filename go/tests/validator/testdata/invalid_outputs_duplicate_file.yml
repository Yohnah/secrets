metadata:
  profile: "test-duplicate-file"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Production/Database"
      key: "Password"

outputs:
  - file: ".env"
    environment: "production"
    format: "dotenv"
  - file: ".env"
    environment: "production"
    format: "dotnet"
