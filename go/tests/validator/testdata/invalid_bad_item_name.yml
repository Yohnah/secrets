metadata:
  profile: "myapp"
  default_environment: "production"

environments:
  production:
    - name: "DB PASSWORD"
      type: "envvar"
      entry: "/Production/DB"
      key: "Password"

outputs: []
