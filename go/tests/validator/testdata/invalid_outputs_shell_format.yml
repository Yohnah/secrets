metadata:
  profile: "test-invalid-format"

environments:
  production:
    - name: "DB_PASSWORD"
      type: "envvar"
      entry: "/Production/Database"
      key: "Password"

outputs:
  - file: "set-env.sh"
    environment: "production"
    format: "invalid_format"
